export const metadata = {
  title: "Statements | Banklens Nigeria",
  description: "Upload and manage your bank statements for tax computation.",
};

export default function StatementsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
