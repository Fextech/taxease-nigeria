export const metadata = {
  title: "Settings | Banklens Nigeria",
  description: "Manage your account, security, and billing settings.",
};

export default function SettingsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
