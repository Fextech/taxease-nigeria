export const metadata = {
  title: "Annotations | Banklens Nigeria",
  description: "Annotate and categorize your bank transactions.",
};

export default function AnnotationsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
