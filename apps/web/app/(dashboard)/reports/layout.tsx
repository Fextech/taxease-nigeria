export const metadata = {
  title: "Reports | Banklens Nigeria",
  description: "Generate and view your PITA tax compliance reports.",
};

export default function ReportsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
