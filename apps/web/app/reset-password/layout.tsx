export const metadata = {
  title: "Reset Password | Banklens Nigeria",
  description: "Reset your Banklens Nigeria password.",
};

export default function ResetPasswordLayout({ children }: { children: React.ReactNode }) {
  return children;
}
