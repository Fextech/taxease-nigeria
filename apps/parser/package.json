{
  "name": "@banklens/parser",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": ".venv/bin/uvicorn app.main:app --reload --port 8000",
    "start": ".venv/bin/uvicorn app.main:app --host 0.0.0.0 --port $PORT"
  }
}
